# Module structure

Every directory is exposed as a top level option in the flake.
Some of these options are standard flake properties and should be familiar.
Non-standard flake options are scoped inside the `neusis` namespace and defined in `neusis-options.nix`.
